pipeline {
    agent any
    enviroment{
        JAVA_HOME '/var/jenkins_home/tools/hudson.model.JDK/java11'
    }
    
    tools {
        jdk 'java11'
        maven 'maven392'
    }
    
    stages {
            stage('Which Java?') {
                steps {
                    sh 'java --version'
                }
            }
            stage ('Build') {
                steps {
                    sh "mvn clean verify"
                }    
            }
    }
}